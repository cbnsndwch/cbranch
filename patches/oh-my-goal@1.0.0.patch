diff --git a/dist/src/goal-store.js b/dist/src/goal-store.js
index 728916b7dad01aa237f8d40dcba04d2e787c54ca..f9cd308bbd1843e42e3e0a28661adf233a68be11 100644
--- a/dist/src/goal-store.js
+++ b/dist/src/goal-store.js
@@ -18,6 +18,7 @@ export const goalSchema = z.object({
     iteration: z.number().int().nonnegative().default(0),
     max_iterations: z.number().int().positive().default(100),
     budget_warning_sent: z.boolean().default(false),
+    session_id: z.string().optional(),
     last_message_id: z.string().optional(),
     last_response_text: z.string().optional(),
     history: z.array(goalHistoryEntrySchema).default([]),
@@ -57,6 +58,7 @@ export function createGoalStore(root) {
             iteration: 0,
             max_iterations: input.maxIterations ?? 100,
             budget_warning_sent: false,
+            session_id: input.sessionID,
             history: [],
         });
     }
diff --git a/dist/src/handlers/command.js b/dist/src/handlers/command.js
index 53a11504eee9d80b0f470a51a8f407b1b3251a26..6d8d3c0a504ebbfd1a40e7ef0c66f13711aa928b 100644
--- a/dist/src/handlers/command.js
+++ b/dist/src/handlers/command.js
@@ -9,9 +9,10 @@ export function createGoalCommandHandler(store) {
             const goal = await store.create({
                 objective: action.objective,
                 tokenBudget: action.tokenBudget,
+                sessionID: extractSessionID(input),
             });
             setToast(output, "Goal started", formatGoalStatus(goal), "success");
-            appendPrompt(output, `Goal started: ${goal.objective}`);
+            appendPrompt(output, `Goal started: ${goal.objective}\n\nWork on this active goal now. Inspect the repository, complete the objective with concrete evidence, and provide the requested recommendation.`);
             return;
         }
         if (action.type === "status") {
@@ -75,13 +76,16 @@ function extractCommand(input) {
         get(input, "event.properties.arguments"),
         get(input, "payload.properties.arguments"),
     ].find((value) => typeof value === "string");
-    if (name === "goal")
-        return `goal ${args ?? ""}`;
-    const candidates = [
+    const command = [
         get(input, "command"),
         get(input, "properties.command"),
         get(input, "event.properties.command"),
         get(input, "payload.properties.command"),
+    ].find((value) => typeof value === "string");
+    if (name === "goal" || command === "goal")
+        return `goal ${args ?? ""}`;
+    const candidates = [
+        command,
         get(input, "arguments"),
         get(input, "text"),
     ];
@@ -110,6 +114,17 @@ function appendPrompt(output, text) {
         return;
     output.prompt = text;
     output.text = text;
+    if (Array.isArray(output.parts)) {
+        output.parts.splice(0, output.parts.length, { type: "text", text });
+    }
+}
+function extractSessionID(input) {
+    return [
+        get(input, "sessionID"),
+        get(input, "properties.sessionID"),
+        get(input, "event.properties.sessionID"),
+        get(input, "payload.properties.sessionID"),
+    ].find((value) => typeof value === "string");
 }
 function get(value, path) {
     return path.split(".").reduce((current, key) => {
diff --git a/dist/src/handlers/loop.js b/dist/src/handlers/loop.js
index 178d5fd77a898b81e7979ff5478a3fc30e9a06e2..3d037007a87c434884384a43bd5dc19e0adfb777 100644
--- a/dist/src/handlers/loop.js
+++ b/dist/src/handlers/loop.js
@@ -18,6 +18,8 @@ export function createGoalLoopHandler(store, client) {
         const goal = await store.read();
         if (!goal || goal.status !== "pursuing")
             return;
+        if (goal.session_id && goal.session_id !== sessionID)
+            return;
         if (goal.tokens_used >= goal.token_budget) {
             await store.setStatus("budget-limited", "Token budget exhausted.");
             setToast(output, "Goal budget exhausted", goal.objective, "warning");
@@ -69,6 +71,13 @@ export function createGoalLoopHandler(store, client) {
         const goal = await store.read();
         if (!goal || goal.status !== "pursuing")
             return;
+        const role = extractString(input, [
+            "info.role",
+            "properties.info.role",
+            "event.properties.info.role",
+        ]);
+        if (role !== "assistant")
+            return;
         const messageID = extractString(input, [
             "info.id",
             "properties.info.id",
@@ -77,6 +86,8 @@ export function createGoalLoopHandler(store, client) {
         if (messageID && messageID === goal.last_message_id)
             return;
         const sessionID = extractSessionID(input);
+        if (goal.session_id && goal.session_id !== sessionID)
+            return;
         const text = await extractResponseText(input, client);
         const tokens = extractTokens(input) ?? estimateTokens(text);
         const achieved = text.match(GOAL_ACHIEVED);
