/// <reference types="vite/client" />

// Provides ambient module declarations for Vite-specific imports used in framework mode,
// notably the `?url` query suffix on stylesheet imports in `root.tsx`
// (e.g. `import appStyles from "./styles.css?url"`).
